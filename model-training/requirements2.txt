# ---------------------------------------------------------------------------
# model-training / requirements2.txt
# ---------------------------------------------------------------------------
# One environment for: feature engineering, training, unpickling models,
# backend inference (prediction_service), and the predictions UI (frontend
# talks to the Flask API — run the backend from ../backend).
#
# From repo root (recommended):
#   python -m venv .venv
#   .venv\Scripts\activate
#   python -m pip install -U pip
#   pip install -r model-training/requirements2.txt
#
# Optional notebook support (data1.ipynb):
#   pip install ipykernel jupyter
# ---------------------------------------------------------------------------

# --- Backend API (auth, DB, CORS) — required for “show on frontend” -------
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
python-dotenv==1.0.0
mysql-connector-python==8.0.33
bcrypt==4.0.1
PyJWT==2.8.0

# --- ML stack: parquet, f1_model.py, pickle load in prediction_service -----
numpy>=1.26.0,<3
pandas>=2.1.0,<3
pyarrow>=14.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
lightgbm>=4.3.0
xgboost>=2.0.0

# --- Regenerate f1_features.parquet from DuckDB / SQL (f1_feature_engineering.py)
duckdb>=1.0.0
